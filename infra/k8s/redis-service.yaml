apiVersion: v1
kind: Service
metadata:
  name: cn-redis
  labels:
    app: cn-redis
spec:
  type: ClusterIP
  selector:
    app: cn-redis
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
